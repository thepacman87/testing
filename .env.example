# Required for real AI edits (preferred)
FAL_KEY=your_fal_api_key_here

# Optional alternatives (used if FAL_KEY is missing)
# REPLICATE_API_TOKEN=your_replicate_token
# OPENAI_API_KEY=your_openai_key

# Set to "true" to force demo/mock mode (no external API calls)
# MOCK_MODE=false
