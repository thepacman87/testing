# PixForge needs NO environment variables for default local mode.
# Everything runs in the browser with open-weight models.

# Optional: unlock Settings → Cloud upgrade (FAL FLUX Kontext)
# FAL_KEY=your_fal_api_key_here

# Optional: force cloud API route into mock responses
# MOCK_MODE=false
